# design/

機能単位のデザイン仕様。`design` スキルが書く。

- 命名: `NNNN-<slug>.md`(対応する spec と同じ id)
- 各 design: 画面 / コンポーネント / 状態(empty・loading・error)/ 使用トークン / a11y。
- pencil の `.pen` を使う場合はここから参照する。
- デザイン原則は `docs/PROJECT.md`、美意識ルールは `docs/rules/design.md`。
